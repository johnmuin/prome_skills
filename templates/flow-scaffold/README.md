# Flow Scaffold

Use this directory as the checklist for new lightweight flows.

Required shape:

```text
flow-name/
├── README.md
├── config_template.sh
├── run_all.sh
├── preflight.sh
├── status.sh
└── step*.sh
```

Start with a human-runnable flow first, then add the LLM-facing skill wrapper.

